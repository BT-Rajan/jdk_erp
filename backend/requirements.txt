fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pymysql>=1.1
alembic>=1.13
passlib[bcrypt]>=1.7
# passlib 1.7.4's bcrypt-backend detection breaks on bcrypt>=4.1 (which
# dropped the __about__ attribute it probes for) -- pin below that.
bcrypt<4.1
python-jose[cryptography]>=3.3
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
email-validator>=2.2
qrcode[pil]>=7.4
python-multipart>=0.0.12
# Fernet encryption for secrets that must be recoverable in plaintext
# (a saved mailbox password, app/core/crypto.py) -- declared directly
# rather than relying on it arriving transitively via
# python-jose[cryptography] above.
cryptography>=41
# The one PDF-generation dependency in this codebase -- used only for
# rendering the official Purchase Order document (docs/modules/purchase_orders.md
# #17), a real, evidenced need (the task's own explicit A4/PDF/email
# requirement), not speculative. A well-established, pure-Python library
# with no system-level dependency (no wkhtmltopdf/headless-browser).
reportlab>=4.0
# The JDK Assistant's Claude provider (app/services/assistant_service.py).
# DeepSeek, the other provider, is plain HTTPS via the standard library.
anthropic>=1.0
