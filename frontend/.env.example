# Copy to .env.local (or .env) for local dev. Vite only exposes
# variables prefixed VITE_ to client code -- see src/lib/apiClient.ts.

# Base URL of the backend API (backend/app/main.py, default port 8989
# per install.sh). No trailing slash.
VITE_API_URL=http://localhost:8989
